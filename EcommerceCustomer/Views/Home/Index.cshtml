@model IEnumerable<EcommerceShared.Models.Category>

@{
    ViewData["Title"] = "Home Page";
}
<div class="text-center">
    <h1>Categories</h1>
    <ul>
        @foreach (var category in Model)
        {
            <li>@category.Name</li>
        }
    </ul>
</div>